name: CI/CD Usuarios

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Build Docker image
        run: |
          docker build \
            -t fiapcloudgamesacr1.azurecr.io/fiapcloudgames-usuarios:latest \
            .

      - name: Login to ACR
        run: |
          az acr login --name fiapcloudgamesacr1

      - name: Push Docker image to ACR
        run: |
          docker push fiapcloudgamesacr1.azurecr.io/fiapcloudgames-usuarios:latest

      - name: Deploy to Azure Web App
        run: |
          az webapp config container set \
            --name fiapcloudgames-usuarios-app \
            --resource-group fiapcloudgames-rg \
            --docker-custom-image-name fiapcloudgamesacr1.azurecr.io/fiapcloudgames-usuarios:latest \
            --docker-registry-server-url https://fiapcloudgamesacr1.azurecr.io
